diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/16-subagents.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/16-subagents.md
index 45302a94..1b7da04e 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/16-subagents.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/16-subagents.md
@@ -166,8 +166,19 @@ description = "Path to write review notes"
 
-### 向活动中的子智能体发送消息
+<a id="sending-messages-to-subagents"></a>
+### 向子智能体发送消息
 
-`send_subagent_message` 工具目前仅根会话可用，而且只能向该会话拥有的活动子智能体发送消息。可选参数 `queue` 控制投递方式：
+`send_subagent_message` 默认关闭，通过 `GROK_ACTIVE_AGENT_MESSAGES` 或 `[features] active_agent_messages` 启用。根会话可以向自己拥有的子智能体发送后续消息；开关启用后，获授权的子智能体也可获得该工具。
 
-- 省略或设为 `false` 时使用 **Steer**。若子智能体空闲，该消息会成为一个受保护的排队轮次；若其正在运行，消息会在下一个安全点注入当前轮次。
-- 设为 `true` 时使用 **Queue**，保留排队轮次行为：消息会作为受保护轮次等待，而不会进入活动轮次。
+- `subagent_id: "parent"` 指向该子智能体仍活动的父子智能体。
+- 持久智能体 ID 指向另一个本地子智能体；符合条件的已完成子智能体会以相同身份恢复。
+
+父级为根会话的子智能体不能向根会话发消息。精选工具集不会提供该工具；不允许此类工具的能力模式也会将其移除。子级发送方每个发送方—目标对最多有 4 条在途消息，每次尝试最多向外发送 32 条，超过限制返回 `QuotaExceeded`。
+
+非活动子智能体始终被唤醒，并以该消息作为下一轮。对活动子智能体，可选 `delivery` 参数控制投递：`steer`（默认）在下一个安全点进入当前轮次；`queue` 等待成为受保护的后续轮次；`interject` 是紧急插话，在最早安全点优先于待处理的引导消息送达，并打断阻塞的后台等待。只结束等待调用，后台工作继续运行。
+
+活动但处于轮次间隙的子智能体会将 `steer` 或 `interject` 转成一个受保护排队轮次并开始处理。旧的 `queue: true` 仍等价于 `delivery: "queue"`；两者同时提供时以 `delivery` 为准。
+
+记录将每次发送显示为一行 Message：结果动词、子智能体标签（类型、persona 或角色）、弯引号包围的描述（只取首行，最多 40 个字符）。行会区分已发送、已排队、已插话、发送中、被拒绝或未确认；发送中显示动画圆点；发送给父级时标为 parent。
+
+折叠行不显示消息和原因。按右方向键（Vim 模式 `l`/`e`）展开请求投递方式、完整消息及拒绝/未确认原因，按左方向键（`h`）折叠。Enter、Ctrl+F 或双击可打开对应子智能体视图。若子智能体不在本会话中创建（例如无界面导出或其他会话的 ID），以 ID 最后 8 个字符标注 `subagent …xxxxxxxx`，展开显示原始 `Subagent ID:`，且无法打开视图。
 
@@ -332,3 +343,7 @@ Grok Build 会在智能体屏幕的侧窗格中显示运行中和已完成的工
 
-如上所述——任务窗格按“Subagents”分组显示子智能体，并提供旋转指示器、耗时以及快速终止或检查的入口。
+如上所述，任务窗格按“Subagents”分组，显示加载动画、耗时和终止/检查入口。按 `h` 切换隐藏已完成与显示全部。
+
+### 停靠面板（启用时）
+
+提示框上方的停靠面板列出子智能体。焦点在停靠面板时，`h` 使用与任务窗格相同的完成项过滤；左/右方向键折叠或展开分区标题。
 
@@ -337,9 +352,11 @@ Grok Build 会在智能体屏幕的侧窗格中显示运行中和已完成的工
 
-从回滚区块或任务窗格打开子智能体时，父级视图会被替换为一个带边框的框架，其中包含子级的完整记录：
+从回滚区、任务窗格或仪表盘行打开子智能体时，带边框的视图替代父视图，显示子级完整记录。标题包含状态、标签、粗体描述、模型、恢复/分叉徽章、活动与耗时，以及 [✗] 关闭按钮。子级的回滚、思考和工具调用在框架内显示；父级任务、待办、停靠和目录面板暂时隐藏。
+
+此视图只用于观察。输入框完全隐藏（高度为零），不能聚焦、输入、暂存或发送后续提示。提示仍归父会话所有；要引导子智能体，请关闭视图，从父级调用 `send_subagent_message`。
+
+仍可滚动、折叠、复制、打开链接和块查看器。`Ctrl+C` 仅取消**当前子级**轮次，不取消父级；`Ctrl+.` / `Ctrl+X` 打开子级快捷键帮助。标题中的 Dashboard 和 `‹`/`›` 仍操作父级。空闲时在块查看器按 Enter 会把选中行引用到父级输入框并关闭视图。
 
-- 框架内的标题栏：状态图标（旋转指示器 / ✓ / ✗）、标签 + 粗体描述 + 模型、可选的“resumed”/“forked”徽章、实时活动 · 已耗时，以及 [✗] 关闭按钮。
-- 子智能体自己的回滚、思考、工具调用和（有限的）提示区域会在框架内渲染。
-- 子智能体视图主要用于观察——通常不能像对父级会话那样，直接向子级发送新的顶层提示。
+仅根会话可用的快捷键在这里不会执行，也不会透传给父级：命令面板、模型/会话选择器、设置、扩展、始终批准、转后台、外部编辑器和 Shift+Tab 模式循环均只触发静默重绘，不显示提示。若显示提示队列，它只是只读镜像，不能编辑、立即发送或删除；队列 RPC 始终以父会话为目标。
 
-使用 `q`、`Esc` 或点击关闭按钮返回父级视图。父级回滚区会继续显示子智能体的状态。
+在普通回滚区按 `q`、`Esc` 或点击 [✗] 返回。若正在搜索，先关闭搜索，再按一次退出子视图。`Ctrl+Q` 始终退出 Grok，不会被吞掉；VS Code 系列使用对应退出键 `Ctrl+D`。关闭后父回滚区仍显示子智能体状态。
 
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/17-sessions.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/17-sessions.md
index 9fbd705b..e3b77438 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/17-sessions.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/17-sessions.md
@@ -36,2 +36,5 @@ Grok 为每个会话使用单独的目录，并按工作目录分组。它会对
   chat_history.jsonl      # 发送给模型的原始聊天消息
+  system_prompt.txt       # 实际发送给模型的已渲染系统提示
+  prompt_context.json     # 渲染系统提示时使用的输入
+  tool_definitions.json   # 最近模型调用的函数工具，不含 MCP server__tool 条目
   plan.json               # TODO/任务列表状态
@@ -47,2 +50,4 @@ Grok 为每个会话使用单独的目录，并按工作目录分组。它会对
 
+`tool_definitions.json` 不包含 MCP 的 `server__tool` 条目，因为模型通过其中列出的 `search_tool` 和 `use_tool` 访问这些工具。
+
 ### 会话标题
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/18-sandbox.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/18-sandbox.md
index cf5b03ee..dbe323cc 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/18-sandbox.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/18-sandbox.md
@@ -62,6 +62,6 @@ grok-zh --sandbox strict
 
-### 全局 hook 的直接写入保护
+### 全局路径的直接写入保护
 
 在 `workspace`、`read-only` 和 `strict`（以及扩展这些基础配置的自定义配置）下，
-内核会**拒绝写入** Grok 用作用户全局 hook 源的直接磁盘路径（在已授予读取权限时
+内核会**拒绝写入** Grok 用作用户全局 hook 源的直接磁盘路径，以及配置和信任文件（在已授予读取权限时
 仍可读取）。内置 `strict` 可以读取 `~/.grok`，但只能写入 CWD、
@@ -72,2 +72,5 @@ grok-zh --sandbox strict
 - `hooks-paths` 中列出的绝对目标（相对路径行会忽略；缺少目标会拒绝启动沙箱）
+- `~/.grok/config.toml`、`~/.grok/trusted_folders.toml`、`~/.grok/managed_config.toml`、`~/.grok/requirements.toml`、`~/.grok/sandbox.toml`（设置、文件夹信任、托管策略、要求和沙箱配置）。
+
+这些配置下相关文件为只读，因此接受文件夹信任、通过 `/model` 切换模型或更改权限模式（`/auto`、Shift+Tab）只对当前会话生效，不保存。要持久保存信任，请在启动沙箱前在该目录执行 `grok-zh --trust`；默认模型或权限模式需直接编辑用户 `config.toml`。
 
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/19-plan-mode.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/19-plan-mode.md
index f611333e..fb3c5bad 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/19-plan-mode.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/19-plan-mode.md
@@ -103,3 +103,3 @@
 
-按 `Tab` 可在预览与提示框之间切换。发送反馈时——无论是内联评论、自由格式备注，还是两者兼有——代理都会收到反馈并修订计划。计划模式会保持活动状态，方便你迭代。
+按 `Tab` 可在预览与提示框之间切换。发送反馈时——无论是内联评论、自由格式备注，还是两者兼有——代理都会收到反馈并修订计划。计划模式会保持活动状态，方便你迭代。 输入框中完整的界面命令（例如 `/feedback <text>` 或 `/compact`）会作为命令执行，不作为修订备注发送，审核视图保持打开。输入框仍有此类命令时，按 `a` 批准会被拒绝；先按 Enter 执行命令或将其删除。
 
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/21-terminal-support.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/21-terminal-support.md
index 7cd07005..6a217bb9 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/21-terminal-support.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/21-terminal-support.md
@@ -182 +182,7 @@ GNU screen 上的 Byobu 支持有限。`/doctor` 会报告 `terminal.byobu-scree
 运行 `/feedback` 进行报告。
+
+## Cmd+Enter 不作为约定的发送或换行快捷键
+
+Grok 只将 `Shift+Enter` 和 `Alt+Enter` 作为约定的换行键。很多终端把 Cmd+Enter 用于全屏，因此换行匹配器排除 SUPER，带 SUPER 的 Enter 也不匹配普通 Enter 发送绑定。当 Kitty 等终端实际传来 SUPER+Enter 时，按键会落到把 Enter 视为换行的文本区，因而仍插入换行。Apple Terminal 则有独立的本机 CoreGraphics 修饰键补救路径，按住 Cmd 时可把看似普通 Enter 的事件处理为换行。
+
+SSH 不传递 Cmd 修饰键，远程端收到的可能是普通 Enter，从而发送消息。草稿非空时底栏显示可用换行组合；SSH 优先显示 Alt+Enter。也可以输入 `\` 再按 Enter，或启用 `/ml`。不要依赖远程 Cmd+Enter 换行。
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/23-dashboard.md b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/23-dashboard.md
index a7dce2f2..b6af4f2e 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/23-dashboard.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/zh-CN/23-dashboard.md
@@ -5,3 +5,3 @@
 
-它不是代理模态框（`/config-agents` / `/agents`——定义和 persona）、会话选择器（`/resume` / `F3`——磁盘上的过往对话），也不是工作流运行 UI（`/workflow runs`）。
+它不是代理模态框（`/config-agents` / `/agents`——定义和 persona）、会话选择器（`/resume` / `Ctrl+R`——磁盘上的过往对话），也不是工作流运行 UI（`/workflow runs`）。
 
@@ -34,2 +34,8 @@
 
+**标题栏**显示新智能体将使用的 Git 分支和工作目录，右侧为状态计数。点击位置或按 `Ctrl+L` 选择其他目录，也可在分派框输入 `/cd <path>`。
+
+**操作行**包括默认选中的“新建智能体”，右侧为“打开历史会话”（仅工作区仪表盘）和工作树开关。列表聚焦或分派框为空时，左右方向键沿这些项目移动并停在两端；输入文字后则编辑草稿。搜索模式下即使输入为空，也按编辑查询处理。Vim 的 `l`/`h` 要求列表聚焦，搜索模式下不执行。Enter 执行当前操作，Esc 回到新建智能体；也可点击，或使用 `/resume`、`Ctrl+W`。Git 仓库中启用工作树后，下次分派会在新工作树中创建智能体。
+
+置顶行在两种分组模式下都保持手动顺序，不因活动或更新时间而移动；使用 `Shift+↑` / `Shift+↓` 重排。
+
 每一行都是一个顶层代理。按状态排序（Needs input → Working → Idle → Inactive → Completed → Failed）时，同一状态的行会相邻；也可以按工作目录排序（Ctrl+G 切换）。**Inactive** 是仅名册中的会话，由其他 pager 进程拥有而本进程尚未加载——属于后台噪声，因此该部分**默认折叠**（使用 → 或点击展开）。
@@ -46,3 +52,3 @@
 
-没有内嵌的分组标题；排序顺序让同状态行相邻，每行的圆点和颜色表示所属组。
+按状态分组时显示可折叠的分区标题；每行的图标和颜色也表明所属状态。
 
@@ -78,3 +84,5 @@
 
-打开一行会在**详情视图**中显示代理的对话：顶部标题（代理名；右侧有 {i}/{n} 循环芯片和 [Dashboard]），下方是全宽对话——没有带边框的模态框，因此内边距与列表视图一致。按键会发送给附加的代理；Esc / Ctrl+\（或 [Dashboard]）返回仪表盘；[‹] / [›] 切换代理。快捷键栏显示 Ctrl+\: back to dashboard。注意：Esc 只返回；在代理内部使用 /exit 会关闭会话（仪表盘 toast：“Session closed”）。
+打开一行会在**详情视图**显示对话。直接复用会话自身的单行标题，不另加标题带：最前面是智能体名称（无名时省略），后接分支和目录；常规状态右侧是 `‹ 2/5 ›`（只有一个智能体时隐藏）以及 `[Dashboard]`。按键作用于附加的智能体；Esc / Ctrl+\ 或 Dashboard 按钮返回仪表盘，`‹`/`›` 切换智能体。Esc 仅返回，`/exit` 则关闭会话并显示“会话已关闭”。
+
+只要启用了仪表盘，每个会话的标题都显示 Dashboard 按钮，不仅限从仪表盘打开的会话；点击等同 Ctrl+\。禁用仪表盘后也移除此按钮。
 
