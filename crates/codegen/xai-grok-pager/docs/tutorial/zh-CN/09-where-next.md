# 下一步

你已经掌握了足以高效工作的内容。想了解更多时：

## 内置帮助

- **`/help`** 或 **`Ctrl+P`** — 可搜索所有命令、快捷键和技能。
- **`/docs`** — TUI 中的完整操作指南（在线文档使用 `/docs web`）。涵盖
  会话、无头模式、子智能体、沙箱、记忆以及更多内容。
- **询问 Grok 本身** — 它可以读取自己的用户指南并自行完成设置。
  试试：“我如何在 CI 中运行你？”或“为 GitHub 添加一个 MCP 服务器”。

## 良好习惯

- 会话会自动保存。使用 `grok-zh -c` 恢复最新会话，或使用 `/resume`
  （`Ctrl+R`）选择一个会话。
- 会话太长而变慢？`/compact` 释放上下文；`/context` 显示上下文使用情况。
- 自动化一切：`grok-zh -p "summarize new TODOs" --output-format json`
  以无头模式运行——非常适合脚本和 CI。
- 使用 `grok-zh update` 从社区独立发布渠道更新；用 `/release-notes` 查看变化。
- 感觉哪里不对？`/feedback <text>` 会立即向服务团队发送反馈，无参数 `/feedback` 打开含已保存草稿的表单；中文版问题请提交到社区 Fork 的问题跟踪器。

## 重新打开本教程

随时输入 **`/tutorial`**。

现在去构建点什么吧。
